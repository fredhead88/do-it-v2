# Planner handover — L-planner-0006 → next Planner context (2026-09-15 ~23:45Z)

## State (ledger is truth; `doit` from `~/.do-it/repos/albert-scott`)
- **UPDATE 2026-09-16 07:20Z — L-charter-0006 is `L1-complete`.** Waves 1–2 merged and shipped by the Executor (master `d45600e84`); `L-spec-0019` bridge-cash-tieout-verifier written 07:19Z (11 ACs); `l1-complete` appended from this context. Nothing is owed from a next Planner on this charter; the close (sweep, charter-review, reap) is the Executor's. The next Planner's work is the next open charter in the money order (`doit` → CHARTER CLOSE / open charters). The paragraph below is the pre-update state, kept for the record.
- **L-charter-0006** (Bridge and Cash proofs) was at ⑥ with eight of nine slots `written` and the ninth held by the one-wave-ahead rule.
- Cut: `content/cut-L-charter-0006.md` — third cut (9 units, 3 waves), audited `L-plan-auditor-0017` (`bad_cut: true`, 10) → `-0018` (`bad_cut: false`, 15) → plan-audit `-0019` (`bad_cut: false`, 15); every finding folded in place, last `cut-written` / `plan-written` 23:13Z. Plan: `content/plan-L-charter-0006.md` (12 sections). ADRs `L-adr-0008` (shared-file rule), `-0009` (MoneyLine, identities by construction), `-0010` (cash partition), `-0016` (contract-drift gate, D34).
- Specs `written` (spec-writer ids in parentheses): wave 1 — `L-spec-0011` money-line-extract (10 ACs), `-0012` advertising-basis-extract (7), `-0013` contract-drift-gate (6); wave 2 — `-0014` bridge-walk-backend (9), `-0015` bridge-walk-frontend (7), `-0016` cash-partition-backend (11), `-0017` cash-partition-frontend (7), `-0018` advertising-basis-surfaces (8). Zero unknowns on all eight. They are the Executor's now.
- **The next Planner's one action on this charter:** when `L-spec-0011`, `-0012`, `-0013` are `accepted`/merged, allocate a spec, build the packet for unit `bridge-cash-tieout-verifier` (hand-built with `mkslot6.py`'s shape until a-40 lands — `doit packet spec-writer` refuses every packet in this charter because sibling specs quote the cut's own `Produces:` line), dispatch it, then `doit append l1-complete L-charter-0006 why:='"…"'` naming all nine slots. Owed criterion: the verifier's R6 production re-run, `wake_at` = the deploy carrying its merge (K=1).
- Owed sweep questions (defaults in the Plan, none blocking; no `escalation-owed` event exists — b-25): Q1 `deposit_walk.py` in the cut (named for veto), Q2 per-row Cash Net figures change under allocation, Q3 Sales keeps its figure + stated basis, Q4 the "unsettled" row, Q5 R6's owed run as verify@, Q6 no third cut-audit / no second plan-audit (named for veto), Q7 cc-ads rows keep today's disclosure (named for veto), Q8 the Profit tile's scoped-SP swap named not changed, Q9 a displayed Bridge figure changes meaning by label — the audit's $4,349.62 is an agency-commission double subtraction (named for veto).
- **Operator-owed, unrelated to the charter:** the b-26 contract edits (`agents/planner.md`, `thinker.md`, `executor.md` gain the Agent tool; `src/test_up.py`, `src/test_think.py` flipped) sit **uncommitted** in `~/do-it-v2` — the auto-mode classifier refused the commit as self-modification. Two commits (`decision(b-26): …`, `fix(tests): …`) and a relaunch of the Planner pane. `test_fold.py` fails on HEAD independently.

## Traps measured this session (pilot record R47–R54; change list a-37…a-40, b-26)
- The contract's seat scan counts dead spawns as unserved (R47); `doit alloc --help` allocates (R48, fourth hit); the pane has no Agent tool so it cannot serve anything (R49); two spawns expired unserved at 0 tokens while the Executor served only its own — re-dispatch with a wide `--timeout` and the Executor pane now serves by hand within ~5 min (R50); cut-audit round 1 is where a monolith's cross-branch imports and top-heavy wave 1 get caught, not the pre-pass (R51); round 2 found fifteen more, three string-checkable (R52); the plan-audit found the Plan contradicting the cut on seam sign / class name / serializer (R53); `doit packet spec-writer` refuses on a sibling spec quoting the cut, and a refusal on stdout will be consumed by a chained dispatch — check `$PK` is a file before dispatching (R54).
- Background waits get killed by the harness's memory heuristic within seconds while the box has 11 GB free; poll in the foreground, bounded at ten minutes.
- `doit spend L-charter-0006`: 14 spawns — 4 expired unserved (unmeasured), 2 Opus audits (6.7 + 4.8 min, 0.85M weighted), 8 Sonnet spec-writers (9.0–13.5 min each, five in parallel, 8.0M weighted). Stage clock: `charter-filed` 09:35 → first `cut-written` 13:38 → eighth `spec-written` 23:39; ~9h of that was two unserved spawns.
